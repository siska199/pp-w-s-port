const MenuProjects = () => {
  return <div>MenuProjects</div>
}

interface TPropsCardMenuProject {
  name: string
  description: string;
  main_image?: string
  features?: string;
  
}

const CardMenuProject = (props: TPropsCardMenuProject) => {
  return <div>SISKA</div>
}
export default MenuProjects
